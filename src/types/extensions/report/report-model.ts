export type ExtensionReport = {
    id: number,
    updatedAt: number,
    updatedById: number,
    createdAt: number,
    createdById: number,

    title: string,
    objective: string,
    fileURL: string,
}
